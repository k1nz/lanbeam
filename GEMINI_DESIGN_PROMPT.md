# UI Redesign Prompt — 文件传输助手

你是一位资深的 UI/UX 设计师。请为以下 Web 应用设计一套全新的现代化界面，需同时包含浅色模式和深色模式。

---

## 应用概述

**名称**: 文件传输助手  
**定位**: 局域网内的文件传输 + 实时文本共享工具  
**用户群体**: 开发者、设计师、办公协作场景  
**技术栈**: React + Tailwind CSS（将使用 CSS 变量实现主题切换）  
**目标设备**: 桌面端 + 移动端（响应式设计，Mobile First）

---

## 响应式设计要求

### 断点策略
- **Mobile**: 320px – 767px
- **Tablet**: 768px – 1023px（可选，优先保证 Mobile + Desktop 两端）
- **Desktop**: 1024px+

### 通用原则
- Mobile First 设计思维：先设计移动端布局，再向桌面端扩展
- 触控友好：可点击区域最小 44×44px，按钮之间留足间距防止误触
- 利用 Tailwind 的 `sm:` / `md:` / `lg:` 断点实现响应式
- 移动端减少信息密度，利用折叠/抽屉/底部弹出等模式收纳次要信息

### 各模块移动端适配

**1. 顶部导航栏**
- 移动端高度适当降低（48-52px）
- 标题文字可适当缩小，Logo 保持可辨识
- 右侧操作入口：图标按钮保持可点击尺寸
- 服务器配置面板在移动端改为底部弹出抽屉（Bottom Sheet）

**2. 标签切换栏**
- 移动端保持顶部标签栏，全宽分布，等分两个标签
- 或考虑将标签栏置于底部（类似 iOS Tab Bar），方便单手操作

**3. 文件传输模块**
- **上传区域**：移动端缩小内边距，拖拽区高度降低，支持点击上传（移动端拖拽体验不佳）
- **文件列表表格**：
  - 移动端表格无法完整展示所有列，需要重新设计：
    - 方案 A：卡片列表模式 —— 每行变为一张卡片，纵向堆叠信息（文件名在上，大小/日期等信息在下方小字展示）
    - 方案 B：横向滚动表格（次选）
  - 操作按钮（下载/删除）在移动端始终可见，不依赖 hover
- **过滤栏**：移动端可改为横向滚动的 pills/chips，或下拉筛选
- **文件树**：移动端展开/折叠的点击区域要足够大

**4. 文本共享模块**
- 文本输入框在移动端占满屏幕高度，提供沉浸式编辑体验
- 顶部工具栏简化：连接状态 + 复制按钮 + 清除按钮，横向排列
- 字符计数固定在底部

**5. Toast 通知**
- 移动端：Toast 从顶部弹出，占满宽度减去左右边距
- 桌面端：保持在右上角

**6. 底部状态栏**
- 移动端：精简为一行小字，或整合进导航栏
- 桌面端：保持完整信息展示

### 移动端特有交互
- 文件列表项支持左滑露出操作按钮（下载/删除）
- 长按文件可触发多选模式
- 下拉刷新文件列表

---

## 当前功能模块

### 1. 顶部导航栏（Sticky Header）
- 左侧：应用 Logo/图标 + 标题「文件传输」
- 右侧：两个操作入口
  - 分享链接按钮（icon only）
  - 服务器配置面板（下拉面板，可配置服务器地址、测试连接）

### 2. 标签切换栏（Tab Bar）
- 两个标签：「文件传输」和「文本共享」
- 当前为 Segmented Control 样式（背景白色卡片上的按钮组）

### 3. 文件传输模块（默认标签）
- **上传区域**：虚线边框的拖拽上传区，支持点击选择或拖拽文件/文件夹
- **待上传文件列表**：显示文件名、大小、上传进度条、移除按钮
- **上传模式选择**：上传时若文件已存在，可选择「跳过」或「覆盖」
- **文件列表区域**：
  - 过滤栏（全部 / 文档 / 图片 / 视频 / 设计文件 / 其他）
  - 树形文件浏览器（支持多级目录）
  - 表头：复选框、文件名、文件大小、上传日期、最后更新、上传者
  - 每项支持下载和删除操作
  - 空状态提示

### 4. 文本共享模块（第二个标签）
- 顶部：连接状态指示器（已连接/未连接）
- 操作按钮：复制全部、清除内容
- 大文本输入区域，实时 WebSocket 同步
- 底部：字符计数

### 5. Toast 通知系统
- 位置：右上角
- 类型：成功 / 错误 / 警告 / 信息
- 每种类型有独特图标和颜色
- 自动消失（5秒）

### 6. 底部状态栏
- 显示当前服务器连接地址和连接状态

---

## 设计要求

### 整体风格 — 严格遵循 Vercel 设计语言

参考 vercel.com、v0.dev、Next.js 官网的设计系统，核心理念是「用最少元素传递最多信息」。

**几何与空间**
- 极其克制的设计，以黑白灰为主调，强调色仅用于关键交互
- 大面积留白，内容呼吸感强，绝不拥挤
- 卡片几乎没有可见边框，仅靠微妙的背景色差或极淡阴影区分层次
- 圆角小且统一（4-6px），拒绝过度圆润
- 布局以清晰的几何分割为主，网格感强

**字体**
- 优先使用 Geist 字体（Vercel 官方字体，Sans Serif），备选 Inter
- 字重对比强烈：标题 bold/600，正文 regular/400，辅助文字 500
- 字号层次分明，标题与正文差距拉大
- 行高宽松（1.5-1.6），增强可读性

**阴影与层次**
- 浅色模式：极少使用阴影，层次靠背景色差区分（bg-white → bg-gray-50/80 → bg-gray-100/80）
- 深色模式：更深邃的黑色背景，层次靠微妙的灰度抬升（bg-black → bg-neutral-900 → bg-neutral-850）
- 卡片悬浮时仅用极淡的环形阴影或 1px 半透明边框，不做浮夸投影
- Hover 状态：背景色轻微变化 + 极短过渡（150ms），不喧宾夺主

**交互与动效**
- 过渡极快且克制：150ms ease，仅用于颜色和透明度变化
- 没有弹跳、缩放等夸张动效
- Focus ring：1-2px 的细环，颜色为强调色，无 offset 或仅 1px offset
- 按钮没有渐变，纯色底色 + hover 时略微加深

**图标**
- 使用 Lucide 图标集，统一 1.25rem（20px）尺寸
- 线条粗细一致（stroke-width: 2），视觉重量均匀
- 颜色跟随文字色，不单独使用高饱和色

### 色彩方案（Vercel 风格）

**浅色模式**
- 页面背景：`#FAFAFA`（极浅灰，非纯白）
- 卡片/面板背景：`#FFFFFF`，与页面背景形成极微妙的差异
- 悬浮层（Popover/Dialog）：`#FFFFFF` + 极淡阴影
- 主文字：`#171717`（neutral-900，几乎纯黑）
- 次要文字：`#737373`（neutral-500）
- 辅助/占位文字：`#A3A3A3`（neutral-400）
- 强调色：`#0070F3`（Vercel Blue），仅用于链接、选中态、主按钮
- 边框：`#E5E5E5` 或直接去掉边框用背景色差替代
- 功能色：绿 `#10B981`、黄 `#F59E0B`、红 `#EF4444`，低饱和度柔和版本

**深色模式**
- 页面背景：`#0A0A0A`（接近纯黑但保留一点灰度）
- 卡片/面板背景：`#171717`
- 悬浮层背景：`#1F1F1F`
- 主文字：`#FAFAFA`
- 次要文字：`#A3A3A3`
- 辅助文字：`#737373`
- 强调色：`#3291FF`（亮蓝，比浅色模式稍亮以提高深色背景可见性）
- 边框：`#2E2E2E`（极暗，若隐若现）
- 功能色相应调亮，保持深色背景上的可辨识性

**两个模式下强调色使用原则**
- 强调色仅占页面 5% 以下的视觉面积
- 大部分交互用灰度完成（hover = 背景微变）
- 蓝色仅用于：主 CTA 按钮、选中/当前态指示、链接、focus ring

### 排版
- 字体：Geist Sans（标题）+ Geist Sans（正文），备选 Inter → 系统字体栈
- 字重用 400（正文）、500（辅助强调）、600（标题）、700（大标题）四档
- 字号体系：12px（辅助）/ 14px（正文）/ 16px（小标题）/ 20px（标题）/ 28px（页面大标题）
- 行高：正文 1.5-1.6，标题 1.3-1.4
- 字母间距保持默认（-0.01em 可微调标题）

### 深色模式特别要求
- 所有颜色通过 CSS 变量切换，Tailwind `dark:` 前缀控制
- 背景三层次：`#0A0A0A`（页面）→ `#171717`（卡片）→ `#1F1F1F`（悬浮）
- 文字对比度符合 WCAG AA（正文 ≥ 4.5:1，大标题 ≥ 3:1）
- 深色模式下几乎不用阴影，层次完全靠背景灰度区分
- 若必须用阴影表示悬浮，使用 `box-shadow: 0 0 0 1px rgba(255,255,255,0.08)` 环形阴影

---

## 输出要求

请生成一张或多张高保真设计稿，覆盖以下内容：

### 主画面：桌面端（浅色模式 + 深色模式并列）
展示文件传输标签页的完整界面：
- 顶部导航栏
- 标签栏
- 上传区域（含几个待上传文件 + 进度条）
- 文件列表（含多级目录树、不同文件类型图标、下载/删除操作）
- 底部状态栏
- 旁边并列展示深色模式版本

### 次画面：移动端适配（浅色模式 + 深色模式并列）
展示同一页面的移动端布局（375px 宽度）：
- 移动端导航栏
- 移动端标签栏
- 文件列表的卡片模式
- 过滤栏的 pill/chip 样式
- 底部状态栏的精简样式
- 旁边并列展示深色模式版本

### 附：关键状态和组件
以小卡片/标注形式展示：
- 空状态（无文件时的占位提示）
- Toast 通知（成功和错误各一个，标注移动端和桌面端位置差异）
- 文本共享模块界面（移动端布局优先）
- Hover / Active / Focus 状态的按钮和列表项
- 移动端特有交互示意：左滑操作、Bottom Sheet
- 深色模式下各组件的对应状态

### 设计标注
- 浅色 / 深色的完整色板对比（主色、背景、文字、边框、功能色）
- 圆角 Token（sm / md / lg 对应数值）
- 阴影 Token（浅色模式阴影 + 深色模式阴影/替代方案）
- 间距尺度（基于 4px/8px 系统）
- 断点标注
- 最小可点击区域标注（44px）

请使用 Figma 或类似工具生成高保真设计稿。
